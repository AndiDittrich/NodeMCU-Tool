/* eslint-disable require-await */

// @TODO add glob expanding based on glob/is-glob package
async function expandExpression(args){

    return args;
}

module.exports = {
    expand: expandExpression
}